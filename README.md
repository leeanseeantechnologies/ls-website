# LeeanSeean Technologies website

A premium company landing page built with React, TypeScript, Vite, Material UI, React Router, and Framer Motion.

## Run locally

```bash
npm install
npm run dev
```

Vite will print the local address to open in your browser.

## Build for deployment

```bash
npm run build
```

The production-ready static files are created in `dist/`.
